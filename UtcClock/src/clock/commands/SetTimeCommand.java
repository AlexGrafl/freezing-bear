package clock.commands;

import clock.UtcClockSingleton;

/**
 * @author Alex
 */
public class SetTimeCommand implements Command{

    @Override
    public void execute() {
        //get Input values
        //UtcClockSingleton.getInstance().setHours(hours);
    }
}
